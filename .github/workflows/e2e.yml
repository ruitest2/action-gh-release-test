name: e2e

on: push

permissions:
  contents: write

jobs:
  e2e-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Release
        uses: softprops/action-gh-release@6d62ca09e3ca8bf1036533cececaee7fa03af27e
        if: github.ref_type == 'tag'
        with:
          files: |
            test-assets/release-notes.md
            test-assets/sample-asset.txt
